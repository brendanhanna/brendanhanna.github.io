Place login cover image here as:

- login-cover.webp (preferred)
- login-cover.png (fallback)

The login hero tries `assets/login-cover.webp` first, then `assets/login-cover.png`, then a gradient fallback.
